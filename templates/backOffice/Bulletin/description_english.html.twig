{% extends 'backOffice/base.html.twig' %}


{% block body %}

    <div class="content-wrapper">
        <div class="row grid-margin">
            <div class="col-lg-12">
                <div class="portlet box">
                    <div class="portlet-header link">
                        <div class="caption"><a href="{{ path('bulletin_liste') }}"  ><i class="fa fa-mail-reply" style="color: red" ></i></a>Description bulletins of information</div>
                    </div>
                    <div class="portlet-body">
                        <div class="row mbm">
                            <div class="col-lg-12">
                                <div class="table-responsive">
                                    <table id="table_id"
                                           class="table table-hover table-striped table-bordered table-advanced tablesorter display">
                                        <thead>
                                        <tr>
                                            <th>newsletter number</th>
                                            <th >Title </th>
                                            <th >Description </th>
                                            <th >Date </th>
                                            <th>Publication</th>
                                            <th>Action</th>
                                        </tr>
                                        <tbody>
                                            <tr>
                                                <td>{{ bulletin.id }}</td>
                                                <td>{{ bulletin.titreEn }}</td>
                                                <td> {{ bulletin.descriptionEn }}</td>
                                                <td>{{ bulletin.dateAt|date('d/m/Y') }}</td>
                                                <td>
                                                    {% if bulletin.publier %}
                                                        <a href="{{ path('bulletin_publier', {'id': bulletin.id}) }}"
                                                           class="btn btn-success btn-xs" title="Activé">
                                                            <i class="fa fa-check"></i>
                                                        </a>
                                                    {% else %}
                                                        <a href="{{ path('bulletin_publier', {'id': bulletin.id}) }}"
                                                           class="btn btn-danger btn-xs" title="Désactivé">
                                                            <i class="fa fa-times"></i>
                                                        </a>
                                                    {% endif %}
                                                </td>
                                                <td>
                                                   {# <a href="{{ path('bulletin_ajouter', {'id': bulletin.id}) }}">
                                                        <button type="button" class="btn btn-info btn-xs mbs"><i
                                                                    class="fa fa-plus"></i>&nbsp;
                                                            Add
                                                        </button>
                                                    </a>#}

                                                    <a href="{{ path('bulletin_modifier', {'id': bulletin.id}) }}" >
                                                        <button type="button" class="btn btn-success btn-xs mbs"><i
                                                                    class="fa fa-edit"></i>&nbsp;
                                                            Edit
                                                        </button>
                                                    </a>
                                                    <a href="{{ path('bulletin_supprimer', {'id': bulletin.id}) }}">
                                                        <button type="button" class="btn btn-danger btn-xs mbs"><i
                                                                    class="fa fa-trash-o"></i>&nbsp;
                                                            Delete
                                                        </button>
                                                    </a>
                                                </td>



                                            </tr>



                                        </tbody>

                                        </thead>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </div>

    </div>

{% endblock body %}